# Paper Research Platform - FastAPI + DSPy Backend
# Telkom University Paper Research API

# COMMON PYTHON UTILITIES
packaging

# Web Framework
fastapi
uvicorn[standard]

# Data Validation
pydantic
pydantic-settings

# AI Framework
dspy-ai

# Additional dependencies for DSPy
litellm[proxy]

# HTTP client (required by DSPy for some operations)
httpx
httpx-sse  # For server-sent events streaming support

# JSON Processing (faster than standard json)
orjson

# Environment Variables
python-dotenv

# Database (PostgreSQL via Supabase)
sqlalchemy[asyncio]
asyncpg
alembic

# Database Connection Pooling
psycopg2-binary

# Vector Search and Embeddings
pgvector
voyageai

# Redis (for conversation session management)
redis[hiredis]>=5.0.0

# Additional utilities that may be needed
python-multipart  # For file uploads (if needed)
aiofiles  # For async file operations
